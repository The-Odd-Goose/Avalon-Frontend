// TODO: make this work with firebase for v2
import React from 'react'

interface Props {
    
}

export const Chatbar = (props: Props) => {
    return (
        <div>
            
        </div>
    )
}
