import React from 'react'

interface Props {
    success: Number,
    turn: Number,
    fail: Number,
    rejections: Number
}

export const Mission = (props: Props) => {

    const { success, turn, fail, rejections } = props;

    // TODO: turn this into smth lol

    return (
        <div>

        </div>
    )
}
