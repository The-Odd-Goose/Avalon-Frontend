import React from 'react'

interface Props {
    
}

export const About = (props: Props) => {
    return (
        <div>
            This is the about page. We'll include the rules + developer contact + other stuff like maybe a video eventually?
        </div>
    )
}
